# NOTE: This file is not used for packaging.
#       We still stick up with setupools & setup.py

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
